import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  list: {
    textDecoration: 'none',
    listStyle: 'none',
    display: 'flex',
    fontSize: 'xs',
    padding: '10px 20px',
    marginLeft: 50
  }
}))
